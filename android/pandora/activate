# this file is mean to be sourced.

cd $(dirname "${BASH_SOURCE[0]}")

if [ ! -d "./venv" ]; then
  python3 -m venv venv
  source ./venv/bin/activate
  ./install.sh
else
  source ./venv/bin/activate
fi
