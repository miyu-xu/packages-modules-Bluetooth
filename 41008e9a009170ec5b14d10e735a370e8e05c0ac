{
  "comments": [
    {
      "unresolved": true,
      "key": {
        "uuid": "ab1086a4_d1a28e00",
        "filename": "system/bta/dm/bta_dm_act.cc",
        "patchSetId": 6
      },
      "lineNbr": 303,
      "author": {
        "id": 1881060
      },
      "writtenOn": "2024-01-09T01:03:36Z",
      "side": 1,
      "message": "I comment without knowing the context this method is being run on but:\n\nIs the \"sniff offload\" different from the \"low power manager\" ?\n\nAnd second question the flag and the property are expected to not change at runtime, meaning `bta_dm_init_pm` should have never been called if flag is on and `kPropertySniffOffloadEnabled` is false. In this case, do we need to call disable ?\n\nOr say otherwise, should the code be like this:\n\n\n```suggestion\n  /* if sniff is offload, no need to handle it in the stack */\n  if (IS_FLAG_ENABLED(enable_sniff_offload) \u0026\u0026\n      osi_property_get_bool(kPropertySniffOffloadEnabled, false)) {\n    LOG_INFO(\"Skip initialize low power manager due to sniff offloading\");\n  } else {\n    LOG_INFO(\"Initialize bluetooth low power manager\");\n    bta_dm_init_pm();\n  }\n```\n\n\n(I do not think that change a lot of the behaviour, I am asking for myself)",
      "range": {
        "startLine": 294,
        "startChar": 1,
        "endLine": 303,
        "endChar": 3
      },
      "revId": "41008e9a009170ec5b14d10e735a370e8e05c0ac",
      "serverId": "85c56323-6fa9-3386-8a01-6480fb634889"
    }
  ]
}